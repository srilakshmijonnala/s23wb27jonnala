extends layout

block content
  h1= title
  p Sri Lakshmi's stunts
  button(type="button",class="btn btn-success", onclick="doReduce()") Press Me
  div(id="reduce") Reduce the value 
  script.
   let count=20
   let change=-1
   function doReduce(){
    count+=change;
    change-=1;
    document.getElementById('reduce').innerHTML=`Count ${count} with change ${change}`
    console.log(count
    )
   }